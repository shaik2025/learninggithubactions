---
name: exercise-1

on:
    push:
        branches:
            - main

jobs:
    learning:
        runs-on: ubuntu-latest
        steps:
            - name: learning
              run: "hello how are you"
